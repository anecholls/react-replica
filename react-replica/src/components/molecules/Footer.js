import { FooterContainer, FooterWrapped } from "../atoms/Footer.elements";
import { BrowserRouter, Router, Route, Routes, Link } from 'react-router-dom';

const Footer = () => {
    return(
        <FooterContainer>
            <FooterWrapped>
                FOOTER
            </FooterWrapped>
        </FooterContainer>


    );
};

export default Footer 
